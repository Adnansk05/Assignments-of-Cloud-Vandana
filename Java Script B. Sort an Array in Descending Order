function sortDescending(arr) {
    arr.sort((a, b) => b - a);
    // For strings, use the following line instead:
    // arr.sort((a, b) => b.localeCompare(a));
}

const numbers = [5, 2, 8, 1, 9];
sortDescending(numbers);
console.log(numbers); // Output: [9, 8, 5, 2, 1]
